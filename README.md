# Demo of `vite-imagetools` metadata bugs(?)

This is a SvelteKit demo of some unexpected behavior using [`vite-imagetools`](https://github.com/JonasKruckenberg/imagetools)

## Running the demo

```bash
npm install
npm run dev -- --open
```
